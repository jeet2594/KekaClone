import { PermissionGuardGuard } from './permission-guard.guard';

describe('PermissionGuardGuard', () => {
  it('should be defined', () => {
    expect(new PermissionGuardGuard()).toBeDefined();
  });
});
